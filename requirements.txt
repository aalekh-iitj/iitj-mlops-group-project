# Pinned versions that work on Python 3.10–3.13.
# IMPORTANT: Python 3.14 is NOT yet supported by torch / transformers wheels as of June 2026.
# The CI + inference workflows run on Python 3.11, so these versions are guaranteed there.
# For local sanity-checking on Python 3.14, follow STEP 1B in the runbook (use pyenv/conda to create a 3.11 env).
datasets>=2.19,<3.0
evaluate>=0.4.2,<1.0
huggingface-hub>=0.24,<1.0
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.4,<2.0
sentencepiece>=0.2.0
tokenizers>=0.15,<1.0
torch>=2.2,<3.0
transformers>=4.41,<5.0
wandb>=0.17,<1.0
# Light dev tooling
flake8>=7.0
